<UserControl x:Class="Lab1.Desktop.Views.TablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="250"
                     Text="{Binding NewTableName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0"
                     ToolTip="Enter table name"/>
            <Button Content="Create"
                    Command="{Binding CreateCommand}"
                    Margin="0,0,8,0"
                    Width="90"/>
            <Button Content="Refresh"
                    Command="{Binding LoadCommand}"
                    Margin="0,0,8,0"
                    Width="90"/>
            <Button Content="Intersect..."
                    Command="{Binding OpenIntersectDialogCommand}"
                    Width="100"/>
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Tables}"
                  SelectedItem="{Binding SelectedTable}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Table Name" Width="250" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Id" Width="400" DisplayMemberBinding="{Binding Id}" />

                    <GridViewColumn Header="Actions" Width="320">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Open Rows"
                                            Command="{Binding DataContext.OpenRowsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,8,0"
                                            Width="100"/>

                                    <Button Content="Columns"
                                            Command="{Binding DataContext.OpenColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,8,0"
                                            Width="100"/>

                                    <Button Content="Delete"
                                            Foreground="Red"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Width="80"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="← Back"
                    Command="{Binding BackCommand}"
                    Width="120"
                    Height="32"/>
        </StackPanel>
    </Grid>
</UserControl>
